services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: llm_bench
      POSTGRES_PASSWORD: llm_bench
      POSTGRES_DB: llm_bench
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
